import styled from "@emotion/styled";

const Container = styled.main`
  display: flex;
  justify-content: center;
  align-items: center;
`;

const Home = () => {
  return (
    <Container>
      <span>Charleypolio</span>
    </Container>
  );
};

export default Home;
